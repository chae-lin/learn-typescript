{
  "compilerOptions": {
    "allowJs": true, //타입스크립트 점진적으로 변환가능하도록 (ts로 다 변환하지 않아도 에러X)
    "target": "ES5",
    "outDir": "./built",
    "moduleResolution": "Node", //promise 인식시켜주기 위해
    "lib": ["ES2015", "DOM", "DOM.Iterable"] //라이브러리 속성
  },
  "include": ["./src/**/*"] //어떤 폴더를 대상으로 어떤 파일을 타입스크립트를 컴파일시킬것인가
}
